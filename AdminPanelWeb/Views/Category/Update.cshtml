@model Category

@{
    ViewData["Title"] = "Update";
}
<div class="subheader py-2 py-lg-4 subheader-solid" id="kt_subheader">
    <div class="container-fluid d-flex align-items-center justify-content-between flex-wrap flex-sm-nowrap">
        <div class="d-flex align-items-center flex-wrap mr-2">
            <h5 class="font-weight-bolder mr-2">Yönetim Paneli</h5>
            <h5 class="font-weight-bolder mr-2">-</h5>
            <h5 class="font-weight-bolder mr-2">Kategori Düzenleme </h5>
        </div>
    </div>
</div>
<div class="d-flex flex-column-fluid">
    <div class="container">
        <div class="card">
            <div class="card-body">
                <form asp-controller="User" asp-action="Update" method="post">
                    <div class="card-body">
                        <input type="hidden" id="Id" name="Id" value="@Model.Id" />
                      
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <div class="form-group row">
                            <div class="col-md-6">
                                <label class="control-label">Adres</label>
                                <input class="form-control" asp-for="Name" value="Text" required />
                            </div>


                            <div class="col-md-6">
                                <label class="control-label">Kullanıcı Adı</label>
                                <input class="form-control" asp-for="ParentCategoryId" value="@Model.ParentCategoryId" required />
                            </div>
                        </div>
                        
                        <div class="row form-group">
                            <div class="col-lg-12">
                                <input type="submit" class="btn btn-primary" value="Güncelle" style="float: right;" />
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>


